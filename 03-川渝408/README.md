# 03 川渝 408

- 根目录 `川渝408.html` / `.xlsx`：川渝对比主文件（xlsx 为 2026-08-21 数据版）。**该 html 的 12 个图表由内联 SVG 函数渲染，可正常显示**（并非坏图）。  
- `图表修复/`：另一条技术路线的 ECharts 版（三栏布局 + 悬停 tooltip + 自适应），**优先打开这里的修复版 HTML**。两者数据逐行一致，仅渲染方式与排版不同。  

全国范围请改看 `04-终极版择校`，不要只用川渝表给外省学校下结论。
