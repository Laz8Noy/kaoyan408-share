# 口径说明（唯一权威）

> 建立日期：2026-09-22 ｜ 目的：消除"同一句话指代不同文件"造成的误读。
> **本文件是院校数据规模的唯一权威口径。其他 README 与本文件冲突时，以本文件为准。**

## 一、为什么需要这份文件

2026-09-22 全仓实测发现：根 `README.md` 说"**249** 个招生单位"、`06-院校数据库/README.md:87` 说"合并后的索引共 **238** 所"、`data/meta.json` 说 `nSchools: 177` —— 三个数字都是真的，但**指代三个不同文件**，文档里没有任何限定语。同时 CodeBrick 校数在文档中出现过 **96 / 95 / 114 / 104** 四个值，实际各有其义（见下表），并非矛盾。

## 二、规模口径总表

| 数字 | 含义 | 出处文件 | 限定语（引用时必须带） |
|---|---|---|---|
| **177** | 择校库院校数 | `06-院校数据库/data/schools/*.json`（磁盘 177 个文件，与 `meta.json` `nSchools` 一致） | "06 择校库" |
| **195** | 招生单位行数 | 177 校的 `units[]` 合计 | "06 招生单位行" |
| **96** | CodeBrick 池院校数 | `10-录取分数统计/data/schools_index.json` | "CodeBrick 池" |
| **95** | 成功并入合并视图的 CodeBrick 校数 | `06/data/score_matrix.json` `stats.nWithCb` | "score_matrix 内 CodeBrick 匹配数" |
| **114** | **Dai408** 校数（**不是** CodeBrick） | `06/data/dai408_scores.json` `nUniversities`（1928 条记录） | "Dai408 池" |
| **104** | 招生单位**行**数（**不是**校数） | `10/data/catalog_2026.json`（104 校 / 547 项目） | "catalog_2026 项目行" |
| **389** | 研招网 408 目录院校数 | `06/data/yz408_catalog.json` `schoolCount`（1570 条 / 939 个招生单位） | "研招网 408 目录" |
| **238** | `177 ∪ CodeBrick ∪ 外部源` 合并视图 | `06/data/score_matrix.json` `stats.nSchools` | "score_matrix 合并视图" |
| **249** | 浏览器索引记录数 = 177 + 72 库外（`CB-分数统计` 62 + `信息补充` 10，均 `file=null`） | `06/data/school_browser.json` `nSchools` | "浏览器索引（含库外记录）" |
| **511** | **统一院校库**全部学校数（177 择校 ∪ 96 CodeBrick ∪ 389 研招网目录 ∪ 王道链接 去重） | `kaoyan408.db` 的 `schools` / `v_school_all`（**本地生成，不入库**） | "统一库全部学校" |
| **34** | **06 ∩ 10**（两库都有的院校，可跨库 JOIN） | `kaoyan408.db` `WHERE in_lib06=1 AND in_lib10=1` | "06∩10" |
| **186** | 终极版择校页院校数 = 148(S) + 38(C) | `04/…20260826.html` 内嵌 `var S`/`var C` | "04-0826 内嵌" |
| **105+81** | 同上另一表述：0824 版 105 校（67+38）+ 0826 新增 81 校 N诺候选 | 根 `README.md` | "终极版择校页（0824 基数 + 0826 候选）" |

**引用规范**：任何地方提到院校数，必须写成 `<数字>（<限定语>）`，例如"**249** 条（浏览器索引，含 72 条库外记录）"。

## 三、正本矩阵（每个主题只有一个正本）

| 主题 | **正本（唯一）** | 下游派生 | 更新触发 |
|---|---|---|---|
| 院校主数据（分数线/名额/录取/改考） | `06-院校数据库/data/schools/*.json` | `school_browser.json`、`score_matrix.json`、`04 html`、`04 xlsx`、`08 html`、`kaoyan408.db` | 09-03 复核 / 简章季 |
| 录取分数分布（408 分位） | `10-录取分数统计/data/schools/*.json` | `04 var CB`、`08 DATA.CB`、`kaoyan408.db` | CodeBrick 快照（不可再生） |
| 085410/22408 择校总览 | `04-终极版择校/…20260826.html` 内嵌 `var S/C` | `04 xlsx`、`08 DATA.S/C`、`school_browser` | 三源整合 |
| 导师信息 | `01-导师与规划/085410_22408_导师信息_20260820.md` | `04 xlsx`「导师研究方向」、`06 tut/tutors`、`kaoyan408.db tutors` | 人工 |
| 考情原始资料 | `07-考情资料/**` | `06 wangdao_links_all.json`、`04` 总览列、`08 DATA.wd/nn/cy` | 人工抓取 |
| 2027 改考动态 | `04-终极版择校/2027改考动态_20260820.md` | `04 xlsx`「2027改考动态」、`kaoyan408.db updates_2027` | 官方公告 |
| 川渝专题 | `03/川渝408.xlsx` + `03/图表修复/…修复版.html` | — | 人工 |
| 招生单位代码 | `06-院校数据库/data/yz408_catalog.json` 的 `code` | `crosswalk.json`、`kaoyan408.db schools.code` | 研招网目录 |
| **全部院校（统一视图）** | **派生**（各正本经 `school_key` 关联合并） | `kaoyan408.db`、`schools_unified.json`、`crosswalk.json` | 任一源变更后重跑 `build_unified_db.py` |
| 推荐器 | ⚠️ **无正本**（`08` 为手工快照） | — | 待建 `build_recommender.py` |

## 四、统一院校库（`kaoyan408.db`）口径

**数据模型：学校 → 专业 → 各类数据**（三层）。学院是 `offerings` 表的属性，不作为层级。

- **主键**：`schools.school_key` = 教育部招生单位代码（5 位），来源 `yz408_catalog.json` 的 `code`
- **未匹配者不编造代码**：`school_key = 'X-<标准名>'`、`code = NULL`、`code_verified = 0`
- **口径**：全部学校 **511**（177 择校库 ∪ 96 CodeBrick ∪ 389 研招网目录 ∪ 王道链接 去重）。
  按数据完备度分三档（`v_school_all.data_level`）：`full`（有择校或分数数据）/ `catalog_only`（仅研招网目录）/ `link_only`（仅王道链接，无 408 数据）
- **若只要"有实质数据"的院校**：`SELECT * FROM v_school_all WHERE in_lib06=1 OR in_lib10=1`（= 238 校）
- **规模**：**24 表 + 2 视图 + 16 索引**。核心表：`schools` / `majors` / `offerings` / `admissions` / `score_lines` / `score_quantiles` / `tutors`(49 行，接 `01` 正本) / `kaoqing` / `updates_2027` / `catalog`(4 年招生目录) / `score_bands`(分数带) / `national_lines`(国家线) / `exam_subjects` / `todo_patch`(待补工单) / `dai408_records` / `field_provenance` / `wangdao_links`
- **脏值已数值化**：`plan` / `retest_cnt` / `admit_cnt` 等复合串（如 `8(一志愿)+12调剂`）解析为 `*_value` 整数 + `*_note` 原文；数值化率 ≥ 99%；无数字的定性值（`待定`/`调剂为主`）落 `fill_types` 枚举
- **重建**：`python 09-生成脚本/build_unified_db.py`（纯 stdlib `sqlite3`，跨平台；幂等，连跑两次结果一致）
- **`kaoyan408.db` 不入库**（已加 `.gitignore`）：它是派生产物，clone 后跑一次脚本即可得；
  仓库内只保留**构建脚本**与 **JSON 导出**（`crosswalk.json`、`schools_unified.json`）

- **已知限制（不强行修补）**：`score_quantiles.offering_id` 回连率 **855 / 5673 = 15.1%**。
  理论上限为 **2098 条（37%）**——因为 `10` 库 96 校中多数不在 `06` 择校库，没有 `offering` 可挂；
  在"有 offering 可挂"的 2098 条里已回连 **855 条（40.8%）**。剩余 1243 条是**专业代码对不上**
  （`10` 的 program 专业码与 `06` 的 unit 专业码口径不同）。**不再按学院模糊回连**：offering 的语义键是
  `(校, 专业, 学院)`，专业不一致时挂上去属语义错误，宁可留 NULL。

## 五、时间语义（三个概念不可混用）

| 概念 | 含义 | 字段/写法 |
|---|---|---|
| **数据截止日** | 数据反映的时点 | `data_cutoff_date`；当前各源：`2026-08-20`（择校库）、`2026-09-06`（CodeBrick）、`2026-09-20`（研招网）、`2026-09-21`（Dai408） |
| **发布/整理日** | 文件生成或复核日 | `published_at` / `generated`；如 `2026-09-03`（复核）、`2026-09-13`（补全）、`2026-09-22`（本方案） |
| **文件版本号** | 文件名里的 `YYYYMMDD` | 仅用于"同主题多版本快照"，且必须配 `deprecated` + `superseded_by` |

**禁止**用文件名日期推断数据时点（如 `_20260820` 的文件可能含 2026 年 9 月补录的数据）。

## 六、权威度分级

| 级 | 定义 | 例 |
|---|---|---|
| **L1 官方** | 校研招网 / 学院官网 / 研招网目录 | `yz408_catalog.json`、`07/录取名单原始材料` |
| **L2 第三方机构** | 具名机构聚合口径 | CodeBrick、N诺、王道、Dai408 |
| **L3 AI 辅助整理** | AI 汇总/推断，需复核 | `04` 总览多数行、`categoryGuess` |
| **L4 网络讨论/推测** | 论坛、百科、热度 | `08` 的 `net`（热度）、`src` 中的"网络热度" |

## 七、已知未闭合项（截至 2026-09-22）

- `offerings` 中 **11 条** `major_code` 为 NULL（`direction` 均为"不区分研究方向"，正文无专业代码）
- **13 所**院校 `code_verified=0`（12 所 06 双非 + 清华大学），未编造代码
- `plan_2026` / `retest_cnt` / `admit_cnt` 三字段 **6 成以上是复合字符串**（如 `8(一志愿)+12调剂`），暂无法数值化
- `score_matrix.json` / `dai408_scores.json` / `yz408_catalog.json` 的生成脚本 `ext/` 输入已缺失 → **不可再生**
- 统一库中 **11 条 `offerings` 无 `major_code`**、**13 所院校 `code_verified=0`**（均未编造）
