# 如何一起完善

欢迎 Issue 和 Pull Request。优先欢迎这些贡献：

1. **纠错**：分数线、科目代码、学院名称、招生人数与官方简章不符
2. **补数据**：补某校 2026/2027 复试名单、机试形式、导师方向（请附官方链接）
3. **改考动态**：某校 085410 改考 / 停招 / 新增学院
4. **体验**：HTML 在手机上的显示问题、表格字段不好用

## 提交建议

- 改数字时，在 PR 里写清：**学校、学院、年份、新值、来源 URL**
- 不要提交王道 PDF、付费课课件、个人证件、聊天记录、API Key
- 大文件（>20MB）不要直接丢进仓库

## 数据口径

- **院校数等规模数字必须带限定语**，唯一权威口径见 [`docs/口径.md`](docs/口径.md)。
  例：写"**249** 条（浏览器索引，含 72 条库外记录）"，不要单写"249 所院校"。
- 目标口径默认是 **085410 人工智能 / 电子信息 · 22408（英二 + 数二 + 408）**
- 同校可能有 11408、自命题、非全，必须分学院标注，不要混成一行
- 空值统一用 `null`，不要用 `"—"` 或空字符串；数值不要存成 `"264"` 这种字符串
- 复合值要拆开：`8(一志愿)+12调剂` → `{value: 8, basis: "一志愿"}` + `{value: 12, basis: "调剂"}`

## 数据冲突怎么裁定（重要）

**两个库数字不一样时，不许用"哪个文件更新"或"哪个看起来合理"来判断，必须取证。**
完整规则与已裁定案例见 [`docs/数据源与冲突裁定.md`](docs/数据源与冲突裁定.md)。

取证顺序（权威度从高到低）：

1. **T1 官方一手** —— 学校研究生院/招生办/学院官网公告、研招网
2. **T2 官方转载** —— 转载官方公告全文并标注"来源：XX研究生院"的站点（如中国考研网）
3. **T3 第三方已整理** —— 已把多校整理成表/库的站点（路灯考研、N诺、启航、中公、新东方、CodeBrick、Dai408…）
4. **T4 仓库内值** —— 仅在 T1~T3 都取不到时使用；T4 之间冲突时优先 `04-…20260826.html`（带人工复核）

两条铁律：

- **必须按 `(学校 + 专业 + 学院)` 三元组比对**。同一校同一专业在不同学院可以有不同复试线（例：北京交通大学 085410 在电信 326 / 自动化 285 / 计算机 273）。只按学校比会误报冲突。
- **裁定结果要落文档**：把「校名/专业/学院/两个值/裁定值/依据 URL/取证日期」追加到该文件的 §4.1，再改正本。

## 统一院校库

`kaoyan408.db` 是**派生**产物（学校 → 专业 → 各类数据 三层模型），由
`python 09-生成脚本/build_unified_db.py` 生成。**勿手改**，改动请落在正本（各源库）后重跑。

该 `.db` **不入库**（已加 `.gitignore`）——clone 后跑一次脚本即得。仓库内只提交构建脚本与
JSON 导出（`crosswalk.json`、`schools_unified.json`）。

## 编码与跨平台约定

- 仓库内脚本一律用 `os.path` + 仓库相对路径，**不要写死盘符**（如以 `C:` 开头的绝对路径）
- 文本文件统一 **UTF-8 无 BOM**、换行 **LF**（由 `.gitattributes` 的 `text=auto` 保证）
- 不要提交 `__pycache__/`、`*.pyc`（`.gitignore` 已覆盖，属预防性提醒）
- `07-考情资料/录取名单原始材料/` 下是网页原文快照，**保持原样不要改**（已在 `.gitattributes` 声明 `-text`）
